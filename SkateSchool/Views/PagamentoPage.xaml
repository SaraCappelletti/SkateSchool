<UserControl x:Class="SkateSchool.Views.PagamentoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SkateSchool.ViewModels" 
             xmlns:local="clr-namespace:SkateSchool.Views" d:DataContext="{d:DesignInstance Type=vm:PagamentoVM}" 
                Background="White"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:PagamentoVM/>
    </UserControl.DataContext>
    <Grid>
        <ComboBox ItemsSource="{Binding Tariffari}" DisplayMemberPath="NumeroLezioni" HorizontalAlignment="Left" Margin="62,110,0,0" VerticalAlignment="Top" Width="120"/>
        <Label Content="Seleziona Tariffario" HorizontalAlignment="Left" Margin="62,79,0,0" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
